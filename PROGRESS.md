# PROGRESS.md — статус проекта

Обновляется после каждого этапа/шага. Новая сессия начинается с чтения этого файла.

## Текущее состояние

**Этап 1 завершён. Создано локальное приложение (`app/`); в него встроен Claude Code: запрос в чате на главной → сборка презентации → предпросмотр в панели справа. Ассеты и правила заполнены в объёме, нужном на текущий момент (решение пользователя от 2026-08-05).**

Работа с дизайн-системой идёт двумя путями:
1. **Приложение** — `cd app && npm start` → http://localhost:3000 — ручная загрузка ассетов, редактирование токенов/правил, создание презентаций (пишет напрямую в файлы проекта).
2. **Импорт из Figma** — файл «Библиотека» (key `SZpOoVhI07GPa3Vf7BRc10`, см. `figma/FIGMA-SOURCES.md`). На 2026-08-04 в нём видна только одна страница «Обложка»; ожидаются страницы с остальными группами ассетов — тогда повторить импорт.

## План этапов

| Этап | Содержание | Статус |
|------|-----------|--------|
| 0 | Проверка Figma MCP (`whoami`) и инструментов (Node, Python, Git) | ✅ done — 2026-08-04 |
| 1 | Скелет проекта: структура папок, CLAUDE.md, заглушки правил | ✅ done — 2026-08-04 |
| 2 | Импорт дизайн-системы из Figma (по шагам, см. ниже) | ⬜ не начат |
| 3 | Правила презентаций (`rules/*`) | 🟡 заполнены по анализу Figma «Презентации», ждут ответов на открытые вопросы — 2026-08-05 |
| 4 | Движок сборки (HTML + Figma) и тестовая презентация `_example` | 🟡 HTML-движок работает: css из токенов, 9 макетов, build + PDF, `_example` собрана — 2026-08-05. Claude встроен в приложение (чат + предпросмотр) — 2026-08-05. Осталось: остальные макеты, Figma-движок |

## Этап 2 — шаги импорта

| Шаг | Что | Статус |
|-----|-----|--------|
| 2.1 | Цвета → `tokens/colors.json` | ⬜ |
| 2.2 | Типографика → `tokens/typography.json`, `FONTS-RULES.md` | ⬜ |
| 2.3 | Сетка и отступы → `tokens/grid.md`, `tokens/spacing.json` | ⬜ |
| 2.4 | Эффекты → `tokens/effects.json` | ⬜ |
| 2.5 | Логотип → `logo/`, `LOGO-RULES.md` | ⬜ |
| 2.6 | Иконки → `icons/`, `ICONS-RULES.md` | ⬜ |
| 2.7 | Паттерны → `patterns/`, `PATTERNS-RULES.md` | ⬜ |
| 2.8 | Мокапы → `mockups/`, `MOCKUPS-RULES.md` | ⬜ |
| 2.9 | Фото (стилистика + каталог) → `photos/`, `PHOTOS-RULES.md` | ⬜ |
| 2.10 | Примеры слайдов → черновик `rules/slide-layouts.md` | ⬜ |

## Журнал

- **2026-08-05 (10)** — Реструктуризация приложения (по запросу пользователя): (1) правая панель «Создать презентацию» удалена — единственная точка входа теперь чат на главной (сетка стала 240px + контент); (2) в футере сайдбара вместо подсказки две иконки Lucide: профиль (попап с ФИО и должностью, хранится в localStorage `factory-profile`) и настройки; (3) сайдбар в двух режимах: по умолчанию история чатов («+ Новый чат», список с удалением), меню токенов/ассетов/правил открывается только кнопкой настроек (⚙ ⇄ «К чатам»); (4) чаты сохраняются на сервере: `app/data/chats/<id>.json` `{id,title,session,items,created,updated}`, эндпоинты GET/POST/PUT/DELETE `/api/chats`; фронтенд создаёт чат при первом сообщении, автосохраняет ленту (запросы, рассуждения, действия, результат) с дебаунсом и восстанавливает диалог вместе с session-id Claude (продолжение работает через `--resume`). Также: заголовок чата чёрный, подпись перенесена под поле ввода, лента и ввод — колонка 760px по центру, как в чате Claude.

- **2026-08-05 (9)** — Claude Code встроен в приложение (вместо механики «заявок», решение пользователя: приложение для коллег, всё делается сразу). Сервер: `app/claude.js` — `POST /api/chat` запускает `claude.exe` headless (`-p --output-format stream-json --verbose`, `--permission-mode acceptEdits` + allowedTools, `--append-system-prompt` с ролью фабрики) в корне проекта и транслирует события в браузер по SSE (рассуждения, действия с инструментами, текст, результат); диалог продолжается через `--resume <session-id>`; `POST /api/chat/stop` останавливает процесс; вложения сохраняются в `input/requests/<метка>/` и передаются путями в промт. Фронтенд: главная — чат по центру (лента: сообщения, блоки «Рассуждение», строки действий, ответы, карточка результата; кнопка отправки ⇄ стоп); по строке `RESULT: output/<имя>` в финальном ответе справа открывается панель предпросмотра готовой презентации (50% ширины, во всю высоту, iframe + «В новой вкладке», контент приложения ужимается влево — как панель артефактов у Claude). Боковая панель «Создать презентацию» и кнопка «Собрать HTML» в редакторе отправляют запрос в этот же чат. Проверено смоук-тестом: стрим и resume работают. Нюанс: серверу нужен перезапуск после обновления кода (`npm start`).

- **2026-08-05 (8)** — Четвёртый раунд правок: (1) лид-блоки шапки прижаты к правому полю сетки (x=1800), ширина по контенту ≤ 733px — на всех макетах; (2) `pain-solution`: бейдж «Pain N» и подпись «Вызов» УДАЛЕНЫ — шапка стала стандартной двухколоночной (H1 в 2 строки + challenge-лид справа на уровне H1); правило «бейдж боли» в style-guide помечено отменённым; (3) длинный H1 переносится в 2 сбалансированные строки (блок ≤ 760px, text-wrap: balance); (4) `intro` — фоновая сцена `[image-bg: macbook-on-desk.png]` как предпочтительный вариант. `_example` пересобрана, PDF обновлён.

- **2026-08-05 (7)** — Третий раунд правок: (1) фото на светлых слайдах — отдельным контейнером: текст-карточка 2/3 ширины + фото-карточка 1/3 с заполнением fill (title-bullets переделан, компонент `.photo-card`); (2) паттерны заменены пользователем на `патерн-1…4.svg` (2000×2000) — каталог и style-guide обновлены, движок берёт `патерн-1` для серого росчерка (обновление: файлы заменены ещё раз → актуальные `патерн-1.1`, `патерн-2`, `патерн-3.1`, `патерн-4`; цвет росчерка на белом — `surface-gray` #EEEFF2, решение пользователя); (3) росчерк всегда ПОД текстом (isolation + z-index); (4) 3D смещается от центра — между карточками, для динамики (`[3d-pos: left|center|right]`, по умолчанию right) — style-guide §4. `_example` пересобрана, PDF обновлён.

- **2026-08-05 (6)** — Второй раунд правок пользователя (внесены в правила и движок): (1) единый кегль текста в карточках — мини-списки тоже `body-middle` 25px; (2) иконки в синих кружках-бейджах увеличены до 34px (кружок 58px без изменений); (3) новое правило 3D: при пустом низе слайда — 3D-объект поверх белых контейнеров по нижней грани с уходом за неё на 20%, 3D на 30–40% слайдов колоды (style-guide §4, пометка `[3d:]` в движке); (4) пояснение к H1 — только лидом в шапке на уровне заголовка, без подписей-«вторых заголовков» («Смысл» удалён из principle-detail); (5) правило баланса строк ~7/5 слов, без висячего хвоста (`text-wrap: balance`). `_example` пересобрана, проверена, PDF обновлён.

- **2026-08-05 (5)** — Правки пользователя по первой сборке, внесены в правила и движок: (1) контент светлых слайдов — всегда в белых карточках (style-guide §3), голый текст только для шапки; (2) внутренний отступ карточки — 40px со всех сторон, новый токен `spacing.card-padding` (решение пользователя); (3) правило «слайд не должен быть полупустым» — заполнять фото из photos/people, иконками из icons/ (style-guide Do); (4) висячие предлоги/союзы переносятся на следующую строку — правило в presentation-rules §7, сборщик ставит неразрывные пробелы автоматически; (5) `title-bullets` получил белый контейнер + `[image:]` (фото справа), `numbered-cards-3` — белые карточки + `[icon:]` у секций. `_example` пересобрана и проверена скриншотом, PDF обновлён.

- **2026-08-05 (4)** — Этап 4, ядро движка: `templates/html/css/` (tokens.css из токенов + layout.css с сеткой 7×6/gutter 25 + components.css), `base.html`, 9 шаблонов макетов (`cover`, `final`, `statement`, `title-bullets`, `intro`, `numbered-cards-3`, `pain-solution`, `benefits-grid`, `principle-detail`), сборщик `scripts/build.js` (md → output/<имя>/, автофинал, проверка лимитов §5, копирование ассетов, перекраска росчерка в light-gray) и `scripts/export-pdf.js` (headless Edge/Chrome, слайд = страница). Тестовая колода `input/_example.md` (9 слайдов) собрана и проверена скриншотом в браузере; PDF выгружен. Попутно правило §0 поймало баг правил: зазоры колонок приведены к gutter 25px из `tokens/grid.md` (3 колонки = 543,3px — совпало с референсом Figma). Осталось в этапе 4: связка с заявками приложения (input/requests/ → build), остальные макеты библиотеки, экран предпросмотра, Figma-движок (push-figma).

- **2026-08-05 (2)** — Пользователь закрыл открытые вопросы правил: тёмный фон = токен `black` #121212 (dark-тема и 3 тёмных макета разблокированы, параметры «стекла» нормированы в style-guide §1); бейдж «Pain N» — канонично заливка `peach-base` + белый текст (исключение зафиксировано в описании токена `peach-soft`); кегли — строго шкала 80/60/50/30/25/20/16 без расширения; шрифт подтверждён (`fonts/files/NunitoSans-Variable.woff2`). Остались открытыми: логотипы клиентов (`logos-wall` в `[v2]`), отдельные скриншоты продукта (вариант `benefits-grid`), роль `text-web` #3A3A3A.

- **2026-08-05 (3)** — Пользователь загрузил ассеты: мокапы (`mockups/files/`: macbook-pro-16-3d, macbook-на-столе, iphone_3d, world-map.svg) и 3D-маскота **Theo** (`photos/3d/theo-аватарка|аналитик-1|аналитик-2.png`, робот для тем ИИ-аналитики). Каталоги заполнены описаниями и применением; макеты `intro`, `essence-cards`, `principles-overview`, `hero-glance`, `map` разблокированы из `[v2]`; Theo внесён в style-guide §5.

- **2026-08-05** — Заполнены `rules/presentation-rules.md`, `rules/slide-layouts.md`, `rules/style-guide.md` по анализу файла Figma «Презентации» (`yMAUPbPKCr689lOOWHywQ8`, Section 1: 62 слайда, колоды «Brand platform & positioning» ×2 ревизии и «Tumodo at a Glance»). Методика: структурный разбор всех слайдов + 15 скриншотов + совет из 5 агентов с перекрёстным рецензированием (отчёт: `council-reports/council-report-2026-08-05.html`, стенограмма там же). Ключевые решения: иерархия источников (tokens > style-guide > layouts > Figma), все кегли привязаны к токенам typography.json, сетка нормирована (540/30, 825/30, 390/40 = 1680), политика переполнения «делить, не ужимать», универсальный макет `title-bullets`, 8 макетов помечены `[v2]` (нет токенов dark-темы / мокапов / логотипов клиентов / карты). Открытые вопросы — в конце каждого файла правил (dark-токены, бейдж боли, промежуточные кегли, мокапы, text-web #3A3A3A, шрифты в fonts/files/). Также: в приложении починён фильтр фото + подпапка «все», карточки фото и поп-ап приведены в порядок; в `patterns/` добавлены описания 4 кривых и `main-pattern.svg` (волновой фон обложек, загружен пользователем).

- **2026-08-04 (вечер)** — Импорт из «Библиотеки» завершён: 300 фирменных иконок (`icon-*`, штрих 1.5px) + каталог; палитра сверена с переменными библиотеки (21 токен, Brand/Neutral/Accent, роли и запреты); `effects.json` (тени Shaddow-Light/Dark, 3 градиента, скругления 8/20/30), `typography.json` (Nunito Sans + Inter, display 144); логотип: 2 компонент-сета × 5 вариантов, `LOGO-RULES.md` (охранное поле X=60px), `logo/catalog.json` (22). Из файла «Сайт» (2555:5035): фото совпало с уже загруженным — каталогизировано. Открытые вопросы — см. отчёт в чате (основной шрифт, основной набор иконок, варианты «-20», file key библиотеки «Презентации», #0055FF vs #005AFF).

- **2026-08-04** — Проверен Figma MCP (пользователь: Artem, команды: aartem301101's team / Tumodo team). Создан скелет проекта (этап 1). Ожидание команды на этап 2.
- **2026-08-04** — Создано локальное приложение `app/` (Node/Express, http://localhost:3000): загрузка ассетов с описаниями «зачем/когда», редакторы токенов (цвета, типографика, отступы, эффекты), правил (md) и презентаций (input/output). Дизайн приложения — по стайл-референсу «graphite ink» от пользователя.
- **2026-08-04** — Импорт из Figma «Библиотека» (SZpOoVhI07GPa3Vf7BRc10): в файле пока одна страница «Обложка»; выгружены 2 волновых паттерна в `design-system/patterns/svg/` + catalog.json. Наблюдения (в токены НЕ записано, ждёт подтверждения): фон обложки #0055FF, волны #237BFF, текст #FFFFFF; заголовок Nunito Sans Medium ~144px. Figma variables/styles в файле отсутствуют.
