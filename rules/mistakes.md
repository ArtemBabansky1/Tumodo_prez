# Журнал антипаттернов

Каждая правка пользователя по качеству слайдов фиксируется здесь — навсегда.
Перед каждой сборкой GPT/Codex читает этот файл целиком и проверяет готовые слайды
по колонке «Как надо».

Правило кодификации: получил правку → сначала исправь причину в движке
(шаблон в `templates/html/`, CSS, правило в `rules/`), затем добавь строку сюда
со ссылкой на место фикса. Правка, оставшаяся только в чате, считается потерянной.

| Дата | Плохо (как было) | Как надо | Где закодировано |
|------|------------------|----------|------------------|
| 2026-08-07 | Абсолютный контейнер карточек с top+bottom получал `height:100%` от класса `.cols` — карточки вылезали за нижний край, контент с распоркой уезжал за кадр | На контейнерах с top+bottom всегда `height:auto` | templates/html/layouts/{pain-solution,numbered-cards-3,title-bullets}.html |
| 2026-08-07 | Скриншоты слайдов путались местами/ловили удалённый файл: Edge делегирует навигацию фоновому процессу и возвращает управление до загрузки | Отдельный tmp-html на слайд, свежий `--user-data-dir`, удаление tmp только после всех снимков | scripts/screenshot.js |
| 2026-08-07 | Микрошаблонизатор не понимает точечные пути (`{{side.title}}`) — блок молча пустой | В контекст рендера класть только плоские ключи (`sideTitle`, `sideItems`) | scripts/build.js (benefits-grid) |
| 2026-08-07 | Шаблоны не совпадали с фирменными эталонами (сетка карточек-ячеек вместо синих карточек услуг, 3 карточки вместо «левая колонка + карточки» и т.д.) | Геометрия каждого макета выверена по `design-system/canon/layouts/<layout>.png`; новые семейства: split-left/split-right, service-grid, acc-1/2/3, stmt, divider-title | templates/html/layouts/*, css/{layout,components}.css |
| 2026-08-16 | Заголовок отдельного слайда был крупнее и стоял выше заголовков соседних слайдов | Все H1 содержательных слайдов используют `page-title` и системный якорь семейства; случайный inline `top` запрещён | rules/agent-workflow.md §4; templates/html/layouts/{benefits-grid,title-bullets}.html; scripts/validate-design.js |
| 2026-08-16 | Короткий список сначала растягивался в пустой контейнер, а затем был ошибочно сжат в маленькую плавающую карточку | При заполнении ниже 55% контейнер не растягивается и не сжимается: меняется семейство макета; 3–6 пунктов с фото → полноразмерный `photo-list` | rules/designer-reasoning.md §5.1; scripts/build.js; templates/html/layouts/photo-list.html |
| 2026-08-16 | «Раньше / С Tumodo» были оторваны от строк, а сравнение собиралось отдельными карточками со стрелками | Сравнение — единая таблица: заголовки внутри сетки, три общие колонки, hairline-разделители, без стрелок; фото и таблица одной высоты | templates/html/layouts/comparison-flow.html; css/components.css |
| 2026-08-16 | В правых нижних углах карточек шагов автоматически появлялись синие полукруги | Декор без функции запрещён; автоматический псевдоэлемент удалён | rules/designer-reasoning.md §6; css/components.css → `.process-step` |
| 2026-08-16 | 3D в KPI-карточке поднимал одну подпись выше соседних | KPI сохраняет общий верх чисел и общий низ подписей; ассет размещается в оставшейся безопасной зоне | css/components.css → `.metric-card.has-card-3d`; scripts/validate-design.js |
| 2026-08-16 | Тёмные слайды были разделены светлым, хотя образовывали одну смысловую главу | Все dark-слайды идут подряд с общим `theme-chapter`; разрыв — штраф strict-сборки | rules/agent-workflow.md §2; scripts/build.js |
| 2026-08-17 | Короткие предлоги и союзы могли остаться в конце строки, особенно в HTML overrides | Перед рендером все текстовые узлы проходят типограф: служебное слово связано со следующим неразрывным пробелом | scripts/lib/typograph.js; scripts/build.js |
| 2026-08-17 | Figma DOM capture нормализовал неразрывные пробелы из HTML, поэтому после переноса снова появлялись висячие предлоги | После захвата повторно типографировать `characters` всех TEXT-слоёв через Figma API и запрещать success, пока аудит не покажет 0 обычных пробелов после коротких служебных слов | app/agent.js; rules/agent-workflow.md |
| 2026-08-17 | Вводная фраза в правой части шапки имела левое выравнивание и визуально зависала внутри колонки | Правый lead заканчивается ровно на правой границе сетки и выровнен по правому краю | templates/html/css/layout.css → `.h1-row .lead` |
| 2026-08-17 | В одной карточке 3D ставился слева по чётности карточки и сдвигал её текст вправо относительно соседних | В сетке с общим левым текстовым якорем карточный 3D по умолчанию стоит справа; left — только явное решение | scripts/lib/design-intelligence.js; css/components.css |
| 2026-08-17 | Сравнение выглядело как офисная таблица с тяжёлой серой шапкой | Светлая редакционная матрица: белый контейнер, тонкие вертикальные и горизонтальные разделители, синие смысловые акценты | templates/html/css/components.css → `.change-table` |
| 2026-08-17 | Предпросмотр на платформе кропал 16:9-скриншот через `object-fit: cover`, из-за чего правильные поля слайда выглядели сломанными | Скриншот готового слайда показывается целиком через `contain`; геометрию презентации не исправляют по артефакту превью | app/public/style.css → `.engine-result-frame > img` |
| 2026-08-17 | Однородные строки `photo-list` начинались одинаковыми синими точками, хотя на платформе есть смысловые пиктограммы | Каждая строка получает подходящую фирменную иконку из ассетов; точка остаётся только fallback при реальном пробеле каталога | scripts/lib/semantic-icons.js; templates/html/layouts/photo-list.html |
| 2026-08-17 | Разделители сравнительной матрицы упирались в скруглённые границы белого контейнера | Все hairline-линии заканчиваются за 30px до внешней границы контейнера и имеют скруглённые концы | templates/html/css/components.css → `.change-table` |
| 2026-08-17 | Скруглённые контейнеры после сборки и переноса в Figma оставались со стандартным Corner smoothing 0% | Для каждого узла с радиусом больше 0 обязателен Figma `cornerSmoothing = 0.6` (60%, iOS); HTML использует общий `corner-shape: squircle`, а экспорт завершается только после полного аудита | design-system/tokens/effects.json; templates/html/css/components.css; app/agent.js |

<!-- Пример строки:
| 2026-08-06 | Заголовок карточки прижат к верхнему краю | Внутренний отступ карточки 40px со всех сторон | templates/html/css/components.css → .card |
-->
